# CHANGELOG · FDE 课程站点本地化调整记录

> 本仓库内容源自 [Ameng2001/clife-onto-engine](https://github.com/Ameng2001/clife-onto-engine) 的 `docs/tutorial/FDE课程`。
> 本文件记录**所有区别于上游原版的格式与风格调整**，正文内容零删改，所有样式调整均为"包裹式"注入，可还原（见文末）。
>
> **维护规则**：今后每次对页面格式/风格/结构的调整，都必须更新本文件。由维护脚本自动写入（见「自动更新机制」），手工调整时需手动补录。

---

## 变更记录

### 2026-07-31 · md 源文件修复（3 个文件，共 12 处）

`---` 分隔线后缺少空行，导致 pandoc 将分隔线之间的整段内容误判为单列表格（渲染为 6% 宽度窄条，内部 `##`、`- [ ]` 等 markdown 原样残留）。

| 文件 | 修复处数 |
|---|---|
| `工具实操/T5a_本体建模.md` | 8 |
| `工具实操/T5b_设闸与治理.md` | 3 |
| `FDE交付SOP速查卡.md` | 1 |

修复后用 pandoc 3.1.11.1 重新生成对应 HTML，保留原版 pandoc 2.x 默认样式块与原始 `<title>`，确保与其他页面风格一致。

### 2026-07-31 · HTML 排版补丁 v1（63 个页面）

注入方式：每个 pandoc 生成的 HTML 在 `</head>` 前注入 `<style id="fde-wide-layout-patch">`，在 `</body>` 前注入目录栏 JS。范围 = 除 `slides/` 与入口页 `index.html` 之外的全部 HTML。

- 正文栏宽 36em → 56em（宽屏利用）
- 新增左侧固定目录栏：自动提取 h1/h2/h3、点击跳转、滚动高亮当前节、底部「← 返回课程中心」

### 2026-07-31 · 补丁 v2（bug 修复）

- 修正 CSS 规则顺序错误：基础规则后置导致目录栏被 `display: none` 永久覆盖
- 目录栏出现阈值 1500px → 1400px

### 2026-08-01 · 补丁 v3（阅读体验优化）

- 中文字体栈：苹方 / 微软雅黑 / 思源黑体（原为 Georgia 衬线 + 中文回落细宋体）
- 行距 1.5 → 1.85，字号桌面端 18px，正文栏回调至 42em（中文黄金行宽）
- 标题视觉层次：h2 蓝色侧条 + 渐变底色，h3/h4 逐级递减
- 隐藏与正文 h1 重复的 `#title-block-header` 标题块；目录脚本同步剔除该标题
- 表格：斑马纹 + 表头灰底 + 完整框线，强制撑满正文宽度
- 引用块：浅蓝底 + 左侧强调条 + 圆角
- 代码：行内灰底圆角；代码块卡片化（边框 + 独立底色）

### 2026-08-01 · 补丁 v4（宽屏卡片布局）

- 桌面端（≥1100px）：浅灰页面底 + 白色圆角卡片 + 柔和阴影，消除"没排满"的视觉失衡
- 超宽屏（≥1400px）：内容卡片在目录栏右侧剩余空间内动态居中，两侧留白对称
- 目录栏 `z-index` 提升，避免被卡片遮挡

### 2026-08-01 · Pages 运维事件

- 仓库曾切换 私有→公开，导致 GitHub Pages 被自动下线且未恢复；已通过 API 重新启用（`main` 分支根目录）
- 注意：今后每次切换仓库可见性后，需重新确认 Pages 状态

---

## 新增文件（非上游原版）

| 文件 | 用途 |
|---|---|
| `index.html` | 手工制作的统一入口导航页（六大板块） |
| `.nojekyll` | 使 GitHub Pages 正常服务 `_` 开头目录（`_legacy`、`_素材` 等） |
| `.gitignore` | 排除 `node_modules/`、`vite.log` |
| `package.json` / `package-lock.json` | Vite 本地预览服务（`npm run dev`） |
| `CHANGELOG.md` | 本文件 |

## 未触碰的上游内容

- `slides/` 全部课件放映页（自有完整设计体系）
- `figures/` 全部配图
- 除「md 源文件修复」所列 3 个文件外，其余 md 源文件未改一字

## 工作区维护资产（未入仓库，位于 `tutorial/` 与 `tools/`）

| 资产 | 用途 |
|---|---|
| `tutorial/patch_layout.py` | 补丁注入脚本（当前 v4 样式，幂等；新课件同步后重跑即可） |
| `tutorial/fix_layout_v2.py` / `reading_style_v3.py` / `reading_style_v4.py` | 历次样式迁移脚本（存档备查） |
| `tools/pandoc-3.1.11.1/` | 便携版 pandoc，用于 md → HTML 重新生成 |

## 自动更新机制

- `patch_layout.py` 在实际注入补丁后会**自动向本文件追加变更条目**（调用 `tutorial/changelog.py`）
- 手工进行的样式/结构调整，由操作者按既有格式在「变更记录」顶部补录条目

## 还原方法

所有样式调整均为包裹式注入，还原任一页面：

1. 删除 `<style id="fde-wide-layout-patch">…</style>` 样式块
2. 删除 `</body>` 前注入的目录栏 `<script>` 块（以 `fde-toc` 为标识）
3. md 层修复如需还原，可参考上游仓库对应文件
